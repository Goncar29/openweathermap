# Publish disabled - we use deploy.yml
name: Publish (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Publish disabled - using deploy.yml instead'
